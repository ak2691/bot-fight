import { abilityId } from "./AbilityRegistry.js";

const ABILITY_STATS_BY_ID = Object.freeze({
    1: { activationModel: "immediate", damage: 20, cooldownMs: 1000, activeMs: 400, cooldownKey: "swingCooldownMs", range: 92, arcDegrees: 120 },
    2: { activationModel: "immediate", maxCharges: 5, rechargeMs: 5000, reuseCooldownMs: 2000, cooldownKey: "blockCooldownMs", chargeKey: "blockRechargeMs", resourceKey: "blockCharges", arcDegrees: 180 },
    3: {
        activationModel: "immediate",
        ammoMax: 10,
        reloadMs: 3000,
        cooldownMs: 1000,
        activeMs: 1000,
        cooldownKey: "gunCooldownMs",
        chargeKey: "gunReloadMs",
        resourceKey: "gunAmmo",
        damageByRange: [15, 10, 5, 2],
        damageRanges: [100, 300, 500, 700],
        damageRangeMode: "interpolated",
        damageMinDistance: 0,
        damageMaxDistance: 700,
        range: 700,
    },
    5: {
        activationModel: "immediate",
        maxCharges: 4,
        reloadMs: 3000,
        cooldownMs: Math.round(60_000 / 90),
        activeMs: Math.round(60_000 / 90),
        cooldownKey: "fireballCooldownMs",
        chargeKey: "fireballReloadMs",
        resourceKey: "fireballCharges",
        range: 400,
        size: 30,
        speed: 36,
        damage: 15,
        burnDamage: 2,
        burnTickMs: 1000,
        burnDurationMs: 5000,
    },
    18: { cooldownMs: 7000, windupMs: 1000, visualMs: 500, damage: 15, range: 220, knockback: 90, projectile: true, size: 24, speedPerTick: 44, lifetimeMs: 500 },
    19: { cooldownMs: 1500, maxCharges: 1, distance: 150, activeMs: 200, statusDurationMs: 300, durationMs: 200, speedPerTick: 75, trailMs: 300 },
    25: { cooldownMs: 1800, visualMs: 300, damage: 14, range: 100, arcDegrees: 90, passThroughDistance: 50 },
    20: { cooldownMs: 10_000, windupMs: 200, activeMs: 200 },
    4: {
        activationModel: "immediate",
        cooldownMs: 12000,
        cooldownKey: "grenadeCooldownMs",
        damageByRange: [50, 45, 40, 35, 30, 25],
        damageRanges: [0, 8, 22, 36, 50, 64],
        damageRangeMode: "stepped",
        damageMinRadius: 0,
        damageMaxRadius: 70,
        explosionRadius: 70,
        size: 12,
        speed: 32,
        decelerationPerTick: 1.6,
        fuseMs: 1000,
    },
    12: {
        cooldownMs: 700,
        activeMs: 300,
        damageByRange: [8, 6, 4],
        damageRanges: [0, 166.67, 333.33],
        damageRangeMode: "stepped",
        damageMinDistance: 0,
        damageMaxDistance: 500,
        range: 500,
    },
    6: { activationModel: "immediate", cooldownMs: 10000, windupMs: 400, damage: 5, durationMs: 1200, cooldownKey: "stunCooldownMs", arcDegrees: 100, range: 184 },
    7: { cooldownMs: 5000, windupMs: 300, visualMs: 400, damage: 30, range: 115, arcDegrees: 150, bleedDamage: 2, bleedTickMs: 1000, bleedDurationMs: 5000 },
    8: { cooldownMs: 8000, visualMs: 500, damage: 20, radius: 110, knockback: 250 },
    9: { cooldownMs: 7000, windupMs: 500, visualMs: 300, damage: 8, slowDurationMs: 2000, range: 500, projectile: true },
    10: { cooldownMs: 12000, windupMs: 800, visualMs: 300, healing: 15 },
    11: {
        cooldownMs: 10000,
        entity: "mine",
        maxCharges: 1,
        damage: 18,
        damageByRange: [18],
        damageRanges: [0],
        damageRangeMode: "constant",
        damageMinRadius: 0,
        damageMaxRadius: 87.5,
        radius: 87.5,
        size: 24,
        speedPerTick: 22,
        travelDistance: 176,
        lifetimeMs: 20_000,
        explosionVisibleMs: 300,
    },
    13: { cooldownMs: 11000, windupMs: 900, visualMs: 300, damage: 40, shockDamage: 3, shockDurationMs: 3000, shockTickMs: 1000, movementLockMs: 300, range: 900, beam: true },
    14: {
        cooldownMs: 13000,
        entity: "gravity_field",
        maxDamage: 35,
        minDamage: 20,
        damageStep: 5,
        damageByRange: [35, 30, 25, 20],
        damageRanges: [0, 30, 60, 90],
        damageRangeMode: "stepped",
        damageMinRadius: 0,
        damageMaxRadius: 120,
        radius: 120,
        fieldSize: 240,
        speedPerTick: 22,
        travelDistance: 176,
        fuseMs: 3000,
        pullPerTick: 6,
        durationMs: 2000,
    },
    15: { cooldownMs: 12000, windupMs: 1000, durationMs: 2000, interruptMs: 100, waveSpeedPerTick: 150, projectileSize: 225, projectileLifetimeMs: 1200, stunDurationMs: 100 },
    16: { cooldownMs: 13000, durationMs: 4000, visualDurationMs: 300, maxCharges: 3 },
    17: { cooldownMs: 14000, entity: "hunter_drone", durationMs: 6000, maxCharges: 1, hp: 50, size: 28, moveSpeed: 4.5, range: 200, shotCooldownMs: 1000, shotVisualMs: 300, turnStepDegrees: 8, damage: 3 },
    21: { cooldownMs: 18000, delayMs: 3000, pulseMs: 400, zoneSize: 90, zoneLifetimeMs: 3100 },
    22: {
        cooldownMs: 18000,
        delayMs: 1500,
        entity: "orbital_marker",
        damage: 50,
        damageByRange: [50, 13],
        damageRanges: [0, 130],
        damageRangeMode: "continuous",
        damageMinRadius: 0,
        damageMaxRadius: 130,
        radius: 130,
        markerSize: 260,
        explosionVisibleMs: 400,
    },
    23: { cooldownMs: 17000, durationMs: 1500, visualDurationMs: 300 },
    24: { cooldownMs: 18000, windupMs: 1500, entity: "null_zone", durationMs: 5000, radius: 150, fieldSize: 300, travelDistance: 176 },
});

export const ABILITY_STATS = ABILITY_STATS_BY_ID;

export function abilityStats(value) {
    const id = abilityId(value);
    return id == null ? null : ABILITY_STATS[id] ?? null;
}
