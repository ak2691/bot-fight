# These are build-time values for the S3/CloudFront frontend. VITE_ values are
# public and must not contain secrets.
VITE_API_BASE_URL=https://api.example.com
VITE_CLIENT_BUILD_VERSION=replace-with-git-sha
